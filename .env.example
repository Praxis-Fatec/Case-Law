# Read by docker-compose.yml. Every value has a default, so the database also
# starts with no .env at all — copy this file only if you need to change one.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=caselaw
POSTGRES_PORT=5432
